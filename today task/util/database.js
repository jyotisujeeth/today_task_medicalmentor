const Sequelize = require("sequelize");



const sequelize = new Sequelize(
  simpleTask,
  root,
  myself&me
  // DB_NAME = simpleTask,
  // DB_USERNAME = root,
  // DB_PASSWORD= myself&me
  
);

module.exports = sequelize;
